<%= form_for(current_user.active_likeables.find_by(liked_id: @user.id),
             html: { method: :delete }) do |f| %>
  <%= button_tag( :class => "btn btn-default") do %>
    <i class="fa fa-star-o" aria-hidden="true"></i> Unlike
  <% end %>
<% end %>