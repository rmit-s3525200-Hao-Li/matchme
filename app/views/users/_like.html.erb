<%= form_for(current_user.active_likeables.build) do |f| %>
  <div><%= hidden_field_tag :liked_id, @user.id %></div>
  <%= button_tag( :class => "btn btn-warning") do %>
    <i class="fa fa-star" aria-hidden="true"></i> Like
  <% end %>
<% end %>