<% provide(:title, 'Matches') %>

	<div id="main" class="search-result text-center">
  	<div class="row">
     <div class="twelve columns">
       <h1>Your Matches</h1>
       <% if @users.any? %>
         <%= render @users %>
       <% else %>
         <h3>No matches where found near your location. :(</h3>
       <% end %>
     </div> 
  	</div>
  </div>